using FPV.APP.Models;

namespace FPV.APP.Contracts
{
    public interface IProductRepository
    {
     //   Task<string> AddProduct(ProductEntity product);
     //   Task DeleteProduct(string productId);
     //   Task<List<ProductEntity>> GetAllProductsByUserId(string userId);
     //   Task<ProductEntity> GetProduct(string productId);
    }
}